// Re-export from shared config package to maintain backwards compatibility
export {
  SCENARIOS,
  INSTRUMENTATION_MODES,
  BENCHMARK_PRESETS,
  type TestScenario,
  type InstrumentationMode,
  type BenchmarkPreset,
} from '@otel-perf/config';
